apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: testapp
  annotations:
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          serviceName: testapp
          servicePort: 80

